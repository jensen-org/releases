import { expect, test } from '@playwright/test'
test.beforeEach(async ({ page }) => { await page.route('**/api/release', (route) => route.fulfill({ contentType: 'application/json', body: JSON.stringify({ status: 'available', version: '1.0.0', byteSize: 1048576, downloadUrl: 'https://github.com/jensen-org/jensen-release/releases/download/v1/Jensen-arm64.dmg', releaseUrl: 'https://github.com/jensen-org/jensen-release/releases/v1' }) })) })
test('release card is usable', async ({ page }) => { await page.goto('/'); await expect(page.getByRole('heading', { name: 'Jensen for macOS' })).toBeAttached(); const link = page.getByRole('link', { name: /Download for macOS/ }); await expect(link).toHaveAttribute('href', /github.com/); await link.focus(); await expect(link).toBeFocused(); await expect(page.getByLabel('Jensen scanning eye')).toBeVisible() })
